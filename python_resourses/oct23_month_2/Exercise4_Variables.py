# turning input into variable by adding name = in font of input

name = input("What is your name?") # mike
print(name)
# = mike
# turning input, into variable by adding name = in font of input, and asigning name, to the variable, length.

name = input("What is your name?") # mike
length = len(name)
print(length)
# = 4